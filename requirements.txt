# Direct runtime and test dependencies. Edit this file when dependencies change;
# deployments install the fully resolved requirements.lock instead.
Flask==3.0.3
Flask-Sock==0.7.0
argon2-cffi==23.1.0
cryptography==42.0.8
paramiko==3.4.0
gunicorn==22.0.0
gevent==24.2.1
pytest==8.2.2
pytest-timeout==2.3.1
